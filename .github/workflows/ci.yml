name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Lint code
        run: npm run lint

  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Run tests with coverage
        run: npm run test:coverage

      # Debug step to ensure coverage files are generated
      - name: Check coverage files
        run: |
          echo "Listing coverage directory:"
          ls -la coverage

      # Debugging the content of lcov.info
      - name: Display lcov.info content
        run: |
          echo "Displaying content of lcov.info file:"
          cat coverage/lcov.info || echo "lcov.info not found"

  upload-coverage-report:
    runs-on: ubuntu-latest
    needs: coverage
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Upload coverage to CodeClimate
        uses: codeclimate/test-reporter-action@v2
        with:
          coverage_command: 'npm run test:coverage'
        env:
          CC_TEST_REPORTER_ID: ${{ secrets.CC_TEST_REPORTER_ID }}

      # Debugging the CodeClimate upload step
      - name: Check CodeClimate upload status
        run: echo "Uploading coverage to CodeClimate completed."
